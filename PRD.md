# X Reader · 需求文档 (PRD)

> 通勤路上戴耳机自动朗读 X（Twitter）关注博主推文的 PWA 应用

## 一、一句话定义

**"打开 App → 戴耳机 → 走人。App 自动播放我关注的博主的未听推文，中英混读。"**

---

## 二、目标用户 & 场景

- **用户**：我自己（Vikki）
- **场景 A（主场景）**：通勤路上、走路、逛街，戴耳机、锁屏放兜里，让 App 连续朗读关注博主的推文
- **场景 B（次场景）**：工作时不想切走去看 X，让它在后台念给我听
- **验证目标**：一周后判断"通勤听推文"这个习惯值不值得建立

## 三、核心用户流

```
打开 App
  ↓
看到"未听队列"（比如 12 条新推文）
  ↓
点"全部播放"
  ↓
戴耳机、锁屏、放兜里、出门
  ↓
App 依次朗读每条推文（中英混读，火山 TTS）
  ↓
听完的自动标记"已听"、从队列消失
  ↓
锁屏界面显示当前推文标题、可暂停/切换
```

---

## 四、功能范围（MVP）

### 必须有（P0）

1. **订阅管理**
   - 我配置 3-10 个 X 用户名（如 `@elonmusk`）
   - 系统自动抓这些人的最新推文

2. **推文抓取**
   - 后端定时（每小时）从 Nitter RSS 拉取订阅博主的新推文
   - 去重、存到数据库
   - 支持多镜像自动轮询（一个挂了切下一个）

3. **未听队列**
   - PWA 打开显示"未听推文列表"
   - 按时间倒序，显示作者头像、时间、推文前 50 字预览
   - 每条有"已听 / 未听"状态

4. **朗读播放**
   - 点单条播放 or "全部播放"（连续播放整个队列）
   - 火山 TTS 合成音频、前端播放
   - 播放完自动标记已听、跳下一条
   - 支持暂停 / 上一条 / 下一条

5. **锁屏播放（关键）**
   - 用 `MediaSession API` 让 iOS 锁屏界面显示当前推文
   - 支持锁屏 / 耳机按键控制播放暂停、切换

6. **PWA 安装**
   - iOS Safari 打开 → 分享 → 添加到主屏幕
   - 主屏图标 + 独立启动画面（像原生 App）

### 不做（明确砍掉）

- ❌ 用户系统、登录、多用户（就我一个人用）
- ❌ 评论、点赞、回复 X
- ❌ 图片 / 视频朗读
- ❌ 转发链的展开朗读（只读推文本体）
- ❌ 高级筛选（关键词过滤 / 只听某人）→ v2 再说
- ❌ 翻译（中英原文什么语言念什么语言）

### 明确的限制（不是 bug）

- **Nitter 镜像偶尔挂几小时**：那段时间抓不到新推文，但队列里已有的还能正常听
- **iOS PWA 锁屏播放**：需要用户先在解锁状态下点了播放按钮才能后台继续，第一次冷启动不能后台自动开始
- **只读文字**：图片 / 视频只显示"[图片]"字样跳过

---

## 五、技术架构

```
┌─────────────────────────────────────────┐
│  iPhone Safari (PWA)                    │
│  ├─ 未听队列 UI                          │
│  ├─ 播放器 (HTMLAudioElement)            │
│  ├─ MediaSession (锁屏控制)              │
│  └─ Service Worker (离线缓存)            │
└──────────────┬──────────────────────────┘
               │ HTTPS
┌──────────────▼──────────────────────────┐
│  Node.js 后端 (Zeabur 部署)              │
│  ├─ GET /api/feed  → 未听推文列表        │
│  ├─ POST /api/tts   → 火山 TTS 合成       │
│  ├─ POST /api/mark  → 标记已听            │
│  ├─ 定时任务 (每小时抓 Nitter RSS)        │
│  └─ SQLite 存储 (推文 + 已听状态)         │
└──────────────┬──────────────────────────┘
               │
       ┌───────┴────────┬─────────────────┐
       ▼                ▼                 ▼
  Nitter 镜像池     火山 TTS API      订阅列表
  (RSS 拉取)        (语音合成)       (硬编码或数据库)
```

### 技术栈

| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | HTML + Vanilla JS + Tailwind (CDN) | 零构建，改一行刷新就能看到 |
| 后端 | Node.js + Express | 生态成熟、Zeabur 一键部署 |
| 存储 | SQLite (better-sqlite3) | 单文件、无需数据库服务 |
| TTS | 火山引擎豆包语音合成 | 中英混读好、免费额度大 |
| 抓取 | rss-parser + Nitter 镜像池 | 免费替代 X API |
| 部署 | Zeabur | 你已有账号、免费额度够用 |

---

## 六、火山 TTS 集成

- **App ID**: `9472982371`
- **Access Token**: `SVohM44VVdKSRa1zdyfWnxqPLV4lB_P8`
- **Secret Key**: `ZWEGBITzUzroQYWcb4l9KZ4O9cMTLE3o`（本 MVP 不用，Access Token 就够）
- **Cluster**: `volcano_tts`
- **默认音色**: `zh_female_wanwanxiaohe_moon_bigtts`（湾湾小何，中英混读自然）
- **接口**: HTTP 一次性合成 `https://openspeech.bytedance.com/api/v1/tts`

**这三个 Key 会写在后端环境变量里，不会暴露到前端**。

---

## 七、Nitter 镜像池

维护一个镜像列表，代码里按顺序尝试，失败切换：

```js
const NITTER_INSTANCES = [
  'https://nitter.net',
  'https://nitter.privacydev.net',
  'https://nitter.poast.org',
  'https://nitter.cz',
  // 未来可加
];
```

对每个订阅用户名，请求 `{instance}/{username}/rss` 获取 RSS。

---

## 八、验证成功的标准（一周后回看）

- [ ] 通勤 7 天里，有 ≥ 4 天真的戴耳机打开听了
- [ ] 每次听 ≥ 10 分钟
- [ ] 至少有 1 次听到觉得"这条推文有意思、要收藏 / 转发"
- [ ] 火山 TTS 音质你能接受、不会听 3 分钟就想关

**如果 ≥ 3 条 √**：值得升级成原生 App 上 TestFlight  
**如果 < 3 条 √**：验证失败，损失 ¥0，退出

---

## 九、不确定事项 & 后续决策点

| 事项 | 现状 | 决策点 |
|---|---|---|
| X 博主订阅列表 | 待 Vikki 提供 3-5 个 | 部署前需要 |
| Nitter 镜像池稳定性 | 未实测 | 跑一周看挂多少次 |
| 火山 TTS 中英混读效果 | 未实测 | 首次播放后判断音色是否需要换 |
| 长推文分段策略 | 默认整条一次性 TTS | 若单条 > 1000 字再考虑分段 |

---

*文档版本：v0.1 · 2026-08-19 · Vikki + AI 协作*
